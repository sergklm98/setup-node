# Pending setup tasks

- [ ] `systemctl enable --now podman-auto-update.timer`
- [ ] `ln -s /etc/containers/systemd creds/systemd`
- [ ] `ln -s nodes/racknerd creds/current`
- [ ] switch to using `<app>/configure.sh` and `<app>/defaults.env`
